# Pending items for Niranjan (never rendered on the site)

Moved out of live content per FINAL_SHOWDOWN: no TODO tags or placeholders in production. These need YOUR real data before they can go live.

## Saarthi (removed from saarthi.md until real data exists)
1. **Token / cost optimization section** — needs a live deployment with real API traffic. Capture real token spend per session (per sub-agent), find the highest-cost interaction, apply one real technique (caching / prompt compression / model routing), report actual before/after numbers. No estimates.
2. **Infrastructure optimization section** — document actual hosting tier + cost, any scaling decision made against a real bottleneck, and real latency numbers (STT round-trip, LLM response, TTS synthesis).
3. **Cold-start discovery write-up** — your actual Saarthi discovery process: user interviews, sample size, how you validated the trust-first reframe before building.

## Still blocked elsewhere (from STATE.md)
4. `interview/*.md` persona content — chatbot stays honest-but-thin until this lands; tone gate re-runs after.
5. Claude Certified Architect — upload the credential image once the exam is done (page already carries the registered-for-exam description).
6. Saarthi web screenshots — the web tab of the Saarthi view is an honest stub until you clear web screens.
7. `GROQ_API_KEY` in production env if not already set (chat falls back otherwise).
8. Custom domain aliasing (niranjanvsks.xyz) on Vercel if not already pointed.
